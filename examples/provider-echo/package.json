{
  "name": "example-provider-echo",
  "version": "0.0.0",
  "private": true,
  "description": "Internal/legacy provider echo example — not part of the public SDK surface",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "start": "node dist/src/index.js",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@openmerch/provider": "*"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "@types/node": "^20.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
