---
"@openmerch/agent": patch
---

Narrow the public SDK docs to the agent SDK. Provider-facing npm docs are de-emphasized because provider onboarding on OpenMerch is currently operator-managed; the `@openmerch/provider` package and `provider-echo` example are now treated as internal/preview and are no longer presented as part of the public SDK surface.
