# Changesets

This repo uses [Changesets](https://github.com/changesets/changesets) to manage versioning and changelogs for published packages.

To add a changeset when making a change to a published package:

```bash
npx changeset
```

Follow the prompts to describe your change. The changeset file will be committed with your PR.
